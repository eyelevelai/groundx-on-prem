#!/usr/bin/env bash

set -euo pipefail

printf '%s\n' "$*" >> "$FAKE_AWS_LOG"

if [[ "$1" == "eks" && "$2" == "describe-cluster" ]]; then
  if [[ "$FAKE_SCENARIO" == "existing_cluster" ]]; then
    printf '1.31'
    exit 0
  fi
  echo "ClusterNotFoundException" >&2
  exit 254
fi

echo "unexpected aws invocation: $*" >&2
exit 2
