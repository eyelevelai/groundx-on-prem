# Tasks

- [x] Add failing Helm tests for disabled and enabled extract API disruption budgets.
- [x] Add the strict values contract and opt-in PodDisruptionBudget template.
- [x] Enable two-replica availability in the hosted EKS values.
- [x] Mirror source chart changes into `helm/`.
- [x] Run the full Helm validator and verify the hosted render.
- [ ] Deploy through the normal hosted production Helm workflow and verify live readiness.
