suite: "app renders as expected"
values:
  - ../values.yaml
chart:
  appVersion: 0.1.0
  version: 0.1.0

tests:
  - it: golang.yaml
    templates:
      - ../templates/app/golang.yaml
    asserts:
      - matchSnapshot: {}
