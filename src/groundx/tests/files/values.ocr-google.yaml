# credentials points at a throwaway file generated by .build/bin/validate-helm.sh at
# gate time; it is deliberately not committed, since files/ ships in the packaged chart.
layout:
  ocr:
    enabled: true
    type: google
    project: test-project
    credentials: files/ocr/gcv-test.json
