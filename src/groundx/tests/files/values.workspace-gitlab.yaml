workspace:
  enabled: true
  token: test-runner-token
  publishDryRun: false
  gitProvider: gitlab
  gitlab:
    apiBaseUrl: https://gitlab.example.com/api/v4
    tokenSecret:
      name: workspace-gitlab-token
      key: token
