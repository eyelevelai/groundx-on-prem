workspace:
  api:
    ingress:
      enabled: true
      paths:
        - path: /
          pathType: Prefix
          backend:
            service:
              name: custom-workspace-svc
              port:
                number: 80
