suite: "inference renders as expected"
values:
  - ../values.yaml
chart:
  appVersion: 0.1.0
  version: 0.1.0

tests:
  - it: inference.yaml
    templates:
      - ../templates/app/inference.yaml
    asserts:
      - matchSnapshot: {}
