suite: "db renders as expected"
values:
  - ../values.yaml

tests:
  - it: pxc-operator.yaml
    templates:
      - ../charts/dbOperator/templates/deployment.yaml
    asserts:
      - matchSnapshot: {}
  - it: db_cluster.yaml
    templates:
      - ../templates/services/db_cluster.yaml
    asserts:
      - matchSnapshot: {}
