suite: "stream renders as expected"
values:
  - ../values.yaml

tests:
  - it: stream-cluster.yaml
    templates:
      - ../templates/services/stream-cluster.yaml
    asserts:
      - matchSnapshot: {}
  - it: stream-topics.yaml
    templates:
      - ../templates/services/stream-topics.yaml
    asserts:
      - matchSnapshot: {}