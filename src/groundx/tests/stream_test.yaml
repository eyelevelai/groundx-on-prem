suite: "stream renders as expected"
values:
  - ../values.yaml

tests:
  - it: stream_cluster.yaml
    templates:
      - ../templates/services/stream_cluster.yaml
    asserts:
      - matchSnapshot: {}
  - it: stream_topics.yaml
    templates:
      - ../templates/services/stream_topics.yaml
    asserts:
      - matchSnapshot: {}