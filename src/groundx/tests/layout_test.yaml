suite: "layout renders as expected"
values:
  - ../values.yaml
chart:
  appVersion: 0.1.0
  version: 0.1.0

tests:
  - it: layout.yaml
    templates:
      - ../templates/app/layout.yaml
    asserts:
      - matchSnapshot: {}
